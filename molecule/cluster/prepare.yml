---
- name: Prepare
  hosts: all
  gather_facts: true
  tasks:
    - name: Ensure Python 3 is installed
      ansible.builtin.apt:
        name:
          - python3
          - python3-apt
        state: present
        update_cache: true
